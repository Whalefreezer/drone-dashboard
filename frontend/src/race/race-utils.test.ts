/// <reference lib="deno.ns" />
import { assert, assertEquals } from '@std/assert';
import { defaultLeaderboardSortConfig } from './race-utils.ts';
// No longer need type imports or leaderboard imports as the tests using them were moved.

// --- Mock Data (Moved to leaderboard-logic.test.ts) ---

// --- Helper to create a minimal LeaderboardEntry (Moved) ---

// --- Specific Test Entries (Moved) ---

// --- Test Suite (Moved) ---
// All Deno.test blocks were moved to frontend/src/leaderboard/leaderboard-logic.test.ts
